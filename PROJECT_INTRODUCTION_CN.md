# 个人视频网站毕业设计项目介绍（答辩版）

> 项目名称：基于前后端分离与行为数据驱动的个人视频网站系统  
> 项目类型：毕业设计（工程实现 + 轻量智能分析）  
> 文档用途：用于开题/中期/结题汇报、课程答辩陈述、论文“系统实现与实验分析”章节素材

---

## 一、项目背景与研究意义

随着短视频与长视频平台的普及，用户对“内容可得性”和“内容匹配度”的要求显著提高。传统静态视频站往往只解决“播放”问题，但在“内容分发效率、用户留存、运营优化”方面不足。

本项目以“个人视频网站”为场景，围绕三个问题展开：

1. **如何构建一个完整可用的视频平台闭环**（上传、转码、发布、播放、互动、用户中心）；
2. **如何低成本实现数据驱动迭代**（埋点采集、指标统计、可视化分析）；
3. **如何在有限算力和数据规模下实现轻量智能推荐**（TF-IDF + 协同统计 + 热度融合 + A/B 验证）。

因此，本课题的价值不只是“做一个能播视频的网站”，而是验证“**工程可落地的智能分发最小闭环**”。

---

## 二、项目目标与成果概览

### 2.1 项目目标

- 实现前后端分离的现代化视频站；
- 完成视频生命周期管理：上传 -> 转码 -> 播放 -> 互动；
- 建立行为数据采集与分析链路；
- 构建可解释、可回放的轻量推荐机制；
- 提供 A/B 实验能力验证策略效果；
- 形成可演示、可复盘、可扩展的系统架构。

### 2.2 已完成成果

- 视频上传（本地存储）+ FFmpeg 转码（360/720/1080，MP4 + HLS）；
- 管理后台（视频管理、用户管理〔含启用/禁用状态切换〕、实验管理、报表看板）；
- 前台播放与互动（点赞、评论、收藏、观看历史、进度回写）；
- 行为埋点系统（曝光/点击/播放/进度/完播）；
- 热榜与推荐回流（推荐结果快照 + 多级 fallback）；
- A/B 实验（流量分配、实验状态管理、CTR 报表）；
- Dashboard 指标看板（流量趋势、用户增长、播放漏斗）。

---

## 三、系统架构设计

## 3.1 总体架构（前后端分离）

- **前端层（Vue3 + Vite + TS）**：页面交互、埋点触发、管理端运营功能；
- **接口层（Spring Boot REST）**：统一 API 输出结构、权限校验、跨域控制；
- **业务层（Service）**：视频管理、转码调度、推荐计算、AB 实验、用户中心；
- **数据层（MySQL + SQL）**：业务表、事件表、聚合指标表、推荐结果表；
- **任务层（@Scheduled）**：转码任务、热榜重建、推荐重建、封面分析任务。

## 3.2 核心技术栈

### 后端

- Spring Boot 2.7.18
- JDK 17
- JdbcTemplate（主要数据访问）
- MyBatis-Plus（主要用于雪花 ID）
- Flyway（数据库迁移）
- MySQL 8

### 前端

- Vue 3 + TypeScript
- Vite
- Element Plus
- Pinia + Vue Router
- ECharts（Dashboard）

### 音视频与存储

- FFmpeg（转码、截图）
- 本地存储抽象（StorageService + LocalStorageProvider）

---

## 四、业务功能模块介绍

## 4.1 视频管理模块

- 管理员上传视频，系统生成 `videoId/objectKey`；
- 上传完成后写入 `video_file`，生成 `video_transcode_task`；
- 转码完成写入 `video_variant` 与 `video_play_source`；
- 后台支持发布/下线/删除（删除仅允许 offline 状态）。

## 4.2 播放与互动模块

- 前台播放页支持多清晰度切换；
- 互动行为：点赞、取消点赞、评论增删、收藏；
- 用户观看进度自动回写并支持下次恢复。

## 4.3 用户中心模块

- 注册/登录/登出（Session 鉴权）；
- 我的收藏分页查询；
- 观看历史与完播率记录（90% 阈值判定）。

## 4.4 管理运营模块

- 普通用户管理（列表、搜索、手动新增、删除、启用/禁用切换）；
- A/B 实验创建、编辑、启动、停止；
- CTR 报表对比不同变体效果；
- Dashboard 展示播放、DAU、用户增长、漏斗转化。

---

## 五、智能分析模块（重点）

> 这是本项目区别于“普通视频网站课程作业”的核心部分：
> **从行为采集到推荐回流，再到报表验证，形成了可解释的闭环。**

## 5.1 行为数据采集：埋点体系

### 前端采集

- 在首页、播放页等场景触发事件：
  - `exposure`（曝光）
  - `click`（点击）
  - `play`（播放）
  - `progress`（进度）
  - `complete`（完播）
- 本地队列缓存到 `localStorage`，每 5 秒批量上报；
- 单批最多 20 条，失败重试最多 3 次。

### 后端接收

- 统一入口：`POST /api/events/batch`；
- 写入 `event_log`，保留 `visitor_id/video_id/event_type/event_time/ab_variant` 等关键字段；
- 支持幂等 eventId 合成，降低重复写入风险。

### 设计意义

- 解决“用户行为不可见”问题；
- 为热榜、推荐、A/B 报表提供统一数据基座。

---

## 5.2 热榜计算：5 分钟聚合 + 时间窗口榜单

### 调度机制

- `HotRankRebuildWorker` 每 5 分钟触发（默认 cron：`15 0/5 * * * ?`）；
- 构建以下结果表：
  - `metric_video_5m`（视频级聚合）
  - `metric_site_5m`（站点级聚合）
  - `video_hot_rank_5m`（24h / 7d 热榜快照）

### 热度评分思想

对行为赋予不同权重：

- play/click/exposure/complete/like/comment 为正向贡献；
- unlike/comment_delete 为负向贡献；
- 排序后形成窗口榜单。

### 工程价值

- 避免每次请求实时全量统计；
- 通过“快照化”实现低开销高响应。

---

## 5.3 推荐引擎：混合策略实现

推荐不是单一算法，而是三路融合：

1. **内容相似度（Content）**
   - 从视频标题+简介构建 TF-IDF 向量；
   - 计算视频间余弦相似度，落库 `video_similarity`。

2. **协同统计（CF）**
   - 利用近 7 天行为中 `click/play/complete` 的 UV 统计；
   - 归一化后作为协同偏好近似分数。

3. **热度分（Hot）**
   - 优先读取热榜快照分值；
   - 快照缺失时退化到事件日志聚合分值。

### 融合打分（实际代码）

- `ALPHA = 0.6`
- `BETA = 0.2`
- `GAMMA = 0.2`
- `score_total = ALPHA * content + BETA * cf + GAMMA * hot`

### 推荐重建

- `RecommendRebuildWorker` 每小时执行（默认 cron：`0 5 * * * ?`）；
- 结果写入 `recommendation_result(version_hour)`，前台直接按版本读取。

### 回退策略（保证可用性）

当推荐链路异常或结果缺失时：

1. 推荐快照 -> 2) 24h 热榜 -> 3) 已发布视频按时间倒序。

这体现了“**智能优先，稳定兜底**”的工程策略。

---

## 5.4 A/B 实验：数据驱动验证

### 功能实现

- 管理端创建实验及变体（流量比例总和必须 100%）；
- 用户访问时按 `visitorId + experimentId` 哈希分桶确定变体；
- 结果写入 `ab_assignment`（同访客同实验唯一）；
- 埋点带上 `ab_experiment_id/ab_variant`，形成可统计数据。

### 报表指标

- 曝光 UV、点击 UV、CTR（`click_uv / exposure_uv`）；
- 用于验证封面、推荐策略等运营决策是否有效。

---

## 5.5 可视化分析（Dashboard）

- 概览：播放 PV、DAU 峰值、新增用户、热门视频、运行实验；
- 趋势：流量曲线、用户增长曲线；
- 漏斗：曝光 -> 点击 -> 播放 -> 完播。

### 特点

- 支持时间范围筛选；
- 关键查询有异常降级（缺表时返回 0/空），保证后台不“全红”。

---

## 六、数据库设计概览

- **V1（核心视频域）**：`video/video_file/video_transcode_task/video_variant/video_play_source` 等；
- **V2（事件与智能分析域）**：`event_log/ab_experiment/ab_variant/ab_assignment/video_tfidf_profile/video_similarity/recommendation_result`；
- **V3（用户中心与指标域）**：`app_user/user_favorite/user_watch_history/video_like/video_comment/metric_video_5m/metric_site_5m/video_hot_rank_5m`。

设计思路：

- 业务主表与分析表分离；
- 通过聚合快照减少在线计算成本；
- 支持后续扩展更多推荐特征。

---

## 七、演示时如何体现“智能分析模块”

> 建议答辩演示时长 10~15 分钟，其中智能分析部分至少占 40%。

## 7.1 演示前准备（非常重要）

1. 提前导入并确认 V1/V2/V3 表已存在；
2. 提前准备 5~10 条已发布视频；
3. 预先产生部分行为数据（浏览、点击、播放、点赞、评论）；
4. 可提前跑一轮联调脚本，确保接口链路稳定；
5. 演示机保证 FFmpeg 可用。

## 7.2 推荐演示建议步骤

1. 打开首页，展示“推荐列表 + 热榜（24h/7d切换）”；
2. 进入某视频进行播放、进度、点赞、评论等操作；
3. 回到首页刷新，说明推荐与热榜是基于行为更新；
4. 展示推荐 fallback 机制：即使推荐结果缺失也不会空白。

## 7.3 A/B 演示建议步骤

1. 在后台新建实验（A/B 变体 + 流量比例）；
2. 启动实验；
3. 前台触发曝光/点击；
4. 回到报表页展示 CTR 差异，解释“数据驱动决策”。

## 7.4 Dashboard 演示建议步骤

1. 打开概览，先讲核心 KPI；
2. 再讲流量趋势和用户增长；
3. 最后讲漏斗并解释每一层转化意义。

---

## 八、答辩时向老师的讲解话术（可直接使用）

## 8.1 30 秒版本（开场）

“老师好，我这个系统不仅实现了视频网站的基础闭环，还增加了轻量智能分析能力。具体来说，系统会采集用户行为，按 5 分钟聚合形成热榜，并按小时重建推荐结果，同时通过 A/B 实验和 Dashboard 验证策略效果。核心价值是把‘可播放’升级为‘可运营、可优化’。”

## 8.2 2~3 分钟版本（技术重点）

“在智能模块上，我采用了可解释的混合推荐：内容相似度用 TF-IDF + 余弦相似度，协同分使用近 7 天行为 UV，热度分来自热榜快照。最终按 `0.6*content + 0.2*cf + 0.2*hot` 融合排序，写入推荐快照供前台读取。为了保证工程稳定，我做了多级 fallback：推荐缺失时自动回退到热榜，再回退到发布时间排序。此外，A/B 模块通过哈希分流保证同访客分组稳定，CTR 报表用于量化策略收益。”

## 8.3 强调工程能力的表达

- “我不仅实现算法逻辑，还处理了生产工程细节，例如任务调度、失败降级、幂等写入、会话鉴权、跨域和 ID 精度问题。”
- “后端 Long 统一序列化为 String，避免前端 JS 精度丢失，这是互动功能正确性的关键点。”

## 8.4 老师常见追问与建议回答

### Q1：为什么不用深度学习推荐？

A：本课题目标是毕业设计可落地系统。在有限数据和算力下，先实现可解释、可验证、可迭代的轻量方案更符合工程与教学目标。

### Q2：推荐是否实时？

A：采用“准实时 + 快照”模式：埋点实时入库，热榜 5 分钟重建、推荐小时级重建，兼顾成本与时效。

### Q3：如何证明你的智能模块有效？

A：通过 A/B 实验的 CTR 指标和 Dashboard 漏斗数据来验证，形成“策略 -> 数据 -> 迭代”的闭环。

### Q4：系统稳定性如何保证？

A：关键路径有降级策略，推荐/热榜/看板缺失时仍返回可用结果，避免前台空白或后台不可用。

---

## 九、项目亮点总结

1. **闭环完整**：从视频生产到内容消费再到数据反馈，链路完整；
2. **智能可解释**：推荐公式透明、可复盘、可调参；
3. **运营可验证**：A/B + CTR 报表支持策略对比；
4. **工程可落地**：任务调度、降级容错、鉴权与数据一致性处理到位；
5. **可扩展性好**：可继续接入更复杂特征和模型。

---

## 十、后续可拓展方向（论文展望可用）

- 引入用户画像（兴趣标签、时段偏好、设备偏好）；
- 从 TF-IDF 升级到向量检索/语义嵌入模型；
- 加入在线学习与多臂老虎机做探索-利用平衡；
- 增加前端自动化测试与端到端压测；
- 支持对象存储（MinIO/S3）与消息队列解耦转码链路。

---

## 十一、可直接引用的结论段（结题/答辩）

“本项目完成了一个以视频业务为核心、以行为数据为驱动、以智能分析为增值能力的毕业设计系统。系统在功能完整性、数据闭环性和可演示性方面达到预期目标，验证了轻量智能推荐在个人视频平台场景中的工程可行性。通过 A/B 实验与可视化报表，系统具备了持续优化内容分发策略的基础能力，为后续向更高阶推荐系统演进提供了稳定底座。”
